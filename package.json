{
  "name": "photo-game",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "bun build src/main.js --outdir dist --target browser",
    "dev": "bun build src/main.js --outdir dist --target browser && bun run serve.js",
    "watch": "bun build src/main.js --outdir dist --target browser --watch",
    "serve": "bun run serve.js",
    "test": "bun test"
  },
  "dependencies": {
    "phaser": "^3.80.1"
  }
}
