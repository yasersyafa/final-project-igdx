// Supported UI/field-notes languages, the single list both the Settings dialog
// and the Album read from. Add a language here and a matching key in every entry
// of src/data/education.json to offer it everywhere.
export const LANGUAGES = [
  { code: "en", label: "English" },
  { code: "id", label: "Bahasa Indonesia" },
];

export const DEFAULT_LANG = "en";

export default LANGUAGES;
